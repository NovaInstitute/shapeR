% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-property_shape.R
\name{sh_property_shape}
\alias{sh_property_shape}
\title{Construct a SHACL property shape}
\usage{
sh_property_shape(
  id = NULL,
  path,
  constraints = list(),
  nested = list(node = NULL, or = character(), and = character(), xone = character()),
  annotations = list(),
  severity = NULL,
  deactivated = FALSE,
  extras = list(),
  prefixes = NULL,
  base_iri = NULL,
  normalise = FALSE
)
}
\arguments{
\item{id}{Optional identifier for the property shape. May be an IRI or a
blank-node identifier. Can be \code{NULL} if the shape is anonymous.}

\item{path}{A scalar character string giving the value of \code{sh:path}.
For simple paths this is usually an IRI. More complex paths (sequence,
alternative, inverse) can be represented in a later extension.}

\item{constraints}{A list of \code{sh_constraint} objects representing the
constraint components defined on this property shape (e.g. minCount,
maxCount, datatype, class, in, etc.).}

\item{nested}{A list describing nested shapes and logical combinations.
It should typically contain elements:
\itemize{
\item \code{node}: a single shape identifier referenced via \code{sh:node}
\item \code{or}: character vector of alternative shape IDs from \code{sh:or}
\item \code{and}: character vector of shape IDs from \code{sh:and}
\item \code{xone}: character vector of shape IDs from \code{sh:xone}
}}

\item{annotations}{A named list for messages and other annotation properties
(e.g. \code{sh:message}, \code{rdfs:comment}).}

\item{severity}{Optional scalar character string for the default severity
of this shape (e.g. \code{"sh:Violation"}, \code{"sh:Warning"}).}

\item{deactivated}{Logical flag indicating whether the shape is deactivated
(\code{sh:deactivated}).}

\item{extras}{A list for additional fields not yet modelled explicitly.}

\item{prefixes}{Optional named character vector of prefix mappings used to
normalise IRIs.}

\item{base_iri}{Optional base IRI used when resolving relative identifiers.}

\item{normalise}{Logical; if TRUE, CURIEs/relative IRIs are expanded using
\code{prefixes} and \code{base_iri} before being stored.}
}
\value{
An object of class \code{"sh_property_shape"}.
}
\description{
Creates an S3 object representing a SHACL property shape, typically
referenced from a node shape via \code{sh:property}. Property shapes
constrain values reachable via a given \code{sh:path}.
}
\examples{
min_age <- sh_constraint(
  component = "sh:MinCountConstraintComponent",
  params    = list(minCount = 1L),
  scope     = "property"
)

age_datatype <- sh_constraint(
  component = "sh:DatatypeConstraintComponent",
  params    = list(datatype = "xsd:integer"),
  scope     = "property"
)

sh_property_shape(
  id          = "_:ageShape",
  path        = "ex:age",
  constraints = list(min_age, age_datatype)
)

}
