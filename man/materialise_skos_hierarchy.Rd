% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/materialise_skos.R
\name{materialise_skos_hierarchy}
\alias{materialise_skos_hierarchy}
\title{Materialise SKOS broader/narrower hierarchy as rdf:type triples.}
\usage{
materialise_skos_hierarchy(triples, concept_triples = NULL)
}
\arguments{
\item{triples}{Data frame of RDF triples with at least columns \code{subject},
\code{predicate}, \code{object}. Typically the project description fetched from
Fluree or a test fixture.}

\item{concept_triples}{Optional data frame of SKOS concept scheme triples
(e.g. loaded from \code{cdm-concepts.ttl}). When supplied, \code{skos:broader}
relationships are sourced from the union of \code{triples} and
\code{concept_triples}; only \code{triples} is returned augmented.}
}
\value{
\code{triples} with additional \code{rdf:type} rows inferred from the SKOS
hierarchy. Duplicates are removed.
}
\description{
SHACL \code{sh:class} checks \code{rdf:type} directly without hierarchy traversal. This
function prepares an in-memory triples data frame for SHACL validation by
adding \code{rdf:type} assertions inferred from the SKOS concept hierarchy: for
each instance that has \verb{rdf:type <concept>} and \verb{<concept> skos:broader* <ancestor>}, an \verb{rdf:type <ancestor>} triple is added.
}
\details{
SKOS predicates are matched by IRI substring so both angle-bracket and bare
IRI forms are handled. The returned data frame retains the same column
structure as \code{triples}.
}
\examples{
\dontrun{
project <- data.frame(
  subject   = c("<http://ex.org/proj>", "<http://ex.org/proj>",
                "<http://ex.org/tech>"),
  predicate = c("<http://www.w3.org/1999/02/22-rdf-syntax-ns#type>",
                "<http://w3id.org/aiao#isPerformedWith>",
                "<http://www.w3.org/1999/02/22-rdf-syntax-ns#type>"),
  object    = c("<http://independentimpact.org/cdm/CDMProjectActivity>",
                "<http://ex.org/tech>",
                "<http://independentimpact.org/cdm/SolarPV>"),
  datatype  = NA_character_,
  stringsAsFactors = FALSE
)
cdm_scheme <- read_ttl_as_triples("cdm-concepts.ttl")   # helper not shown
augmented  <- materialise_skos_hierarchy(project, cdm_scheme)
# <http://ex.org/tech> rdf:type cdm:RenewableEnergyTechnology is now present.
}

}
