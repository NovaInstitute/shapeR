% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_shacl.R
\name{validate_shacl}
\alias{validate_shacl}
\title{Validate data against SHACL shapes.}
\usage{
validate_shacl(data, shapes)
}
\arguments{
\item{data}{Data graph to validate. Either an \code{\link[rdflib:rdf]{rdflib::rdf}} object or a
data frame with columns \code{subject}, \code{predicate}, \code{object}, and optional
\code{datatype}.}

\item{shapes}{SHACL shapes graph as returned by \code{\link[=sh_shape_graph]{sh_shape_graph()}}.}
}
\value{
A list with two elements: \code{conforms}, a logical flag, and \code{results},
a data frame describing individual validation outcomes (one row per
violation or warning). Supported result columns are \code{focusNode}, \code{shape},
\code{path}, \code{component}, \code{message}, \code{severity}, \code{value}, and \code{scope}.
}
\description{
This is a lightweight validator that supports a subset of SHACL constraint
components and is designed to work with the in-memory shape model produced by
\code{\link[=read_shacl]{read_shacl()}}. The validator accepts data either as an \code{rdflib} graph or a
data frame of triples and dispatches validation to functions based on each
constraint's \code{component} field.
}
\examples{
\dontrun{
shapes <- read_shacl(system.file("extdata", "visualise-shacl.ttl",
                                package = "shapeR"))

data <- data.frame(
  subject = "<http://example.com/res1>",
  predicate = "<http://example.com/name>",
  object = "Alice",
  stringsAsFactors = FALSE
)

validate_shacl(data, shapes)
}

}
