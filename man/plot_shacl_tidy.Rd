% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_shacl_tidy.R
\name{plot_shacl_tidy}
\alias{plot_shacl_tidy}
\title{Plot a tidy SHACL tibble}
\usage{
plot_shacl_tidy(
  shapes,
  layout = "sugiyama",
  engine = c("ggraph", "visNetwork")
)
}
\arguments{
\item{shapes}{A tidy SHACL tibble, typically returned by
\code{\link[=read_shacl_tidy]{read_shacl_tidy()}}.}

\item{layout}{Layout name passed to \code{\link[ggraph:ggraph]{ggraph::ggraph()}} when \code{engine} is
"ggraph". Ignored for \code{engine = "visNetwork"}.}

\item{engine}{Visualisation engine to use. Choose "ggraph" for static plots
or "visNetwork" for an interactive widget.}
}
\value{
A \code{tidygraph::tbl_graph} object, returned invisibly. The plot is
printed as a side effect.
}
\description{
Create a node–edge diagram from the tidy tibble produced by
\code{\link[=read_shacl_tidy]{read_shacl_tidy()}}. Shapes are connected to their property constraints, and
nested shapes referenced by \code{sh:node}, \code{sh:or}, \code{sh:and}, or \code{sh:xone} are
linked from the relevant property node. Shapes with multiple target classes
are supported and will display each target in the shape label.
}
\examples{
tidy_shapes <- read_shacl_tidy(
  "https://raw.githubusercontent.com/IndependentImpact/Bhash/refs/heads/main/ontology/shapes/consensus.shacl.ttl",
  prefer_curie = TRUE
)
tidy_shapes$property <- lapply(tidy_shapes$property, clean_property_constraints)
plot_shacl_tidy(tidy_shapes, engine = "visNetwork")

}
