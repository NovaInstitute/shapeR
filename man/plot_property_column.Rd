% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_property_column.R
\name{plot_property_column}
\alias{plot_property_column}
\title{Plot tidy SHACL property constraints with visNetwork}
\usage{
plot_property_column(shapes, shape_id = NULL)
}
\arguments{
\item{shapes}{A tidy SHACL tibble returned by \code{\link[=read_shacl_tidy]{read_shacl_tidy()}}. Must
include columns \code{id} and \code{property}.}

\item{shape_id}{Optional character vector of shape identifiers to include.
When supplied, only the matching shapes and their property constraints are
plotted.}
}
\value{
A \code{tidygraph::tbl_graph} object, returned invisibly. The associated
\code{visNetwork} widget is printed as a side effect.
}
\description{
Convenience wrapper for visualising the \code{property} list-column produced by
\code{\link[=read_shacl_tidy]{read_shacl_tidy()}} as an interactive network. The function keeps the
shape-level context but focuses on rendering via \code{visNetwork}, making it easy
to inspect property constraints extracted from a SHACL shapes graph.
}
\examples{
\dontrun{
shapes <- data.frame(
  id = "ex:PersonShape",
  property = I(list(list(
    path = "ex:name",
    constraints = list(list(component = "sh:datatype", datatype = "xsd:string"))
  ))),
  stringsAsFactors = FALSE
)

plot_property_column(shapes)
}
}
