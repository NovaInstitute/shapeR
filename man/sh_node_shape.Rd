% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-node_shape.R
\name{sh_node_shape}
\alias{sh_node_shape}
\title{Construct a SHACL node shape}
\usage{
sh_node_shape(
  id,
  targets = list(targetClass = character(), targetNode = character(), targetSubjectsOf =
    character(), targetObjectsOf = character()),
  properties = list(),
  constraints = list(),
  annotations = list(),
  severity = NULL,
  deactivated = FALSE,
  extras = list(),
  prefixes = NULL,
  base_iri = NULL,
  normalise = FALSE
)
}
\arguments{
\item{id}{A scalar character string giving the node shape identifier
(IRI or blank-node ID).}

\item{targets}{A list describing the targeting of the shape. It should
typically contain zero or more of the following elements, each as a
character vector:
\itemize{
\item \code{targetClass}: targets via \code{sh:targetClass}
\item \code{targetNode}: targets via \code{sh:targetNode}
\item \code{targetSubjectsOf}: via \code{sh:targetSubjectsOf}
\item \code{targetObjectsOf}: via \code{sh:targetObjectsOf}
}}

\item{properties}{A list of \code{sh_property_shape} objects owned by this
node shape (typically coming from \code{sh:property} triples).}

\item{constraints}{A list of \code{sh_constraint} objects representing
node-level constraints (e.g. \code{sh:class}, \code{sh:in}, \code{sh:or},
etc. when attached directly to the node shape).}

\item{annotations}{A named list for messages and other annotation properties
(e.g. \code{sh:message}, \code{rdfs:comment}).}

\item{severity}{Optional scalar character string for the default severity
of this shape (e.g. \code{"sh:Violation"}, \code{"sh:Warning"}).}

\item{deactivated}{Logical flag indicating whether the shape is deactivated
(\code{sh:deactivated}).}

\item{extras}{A list for additional fields not yet modelled explicitly.}

\item{prefixes}{Optional named character vector of prefix mappings used to
normalise identifiers.}

\item{base_iri}{Optional base IRI used when resolving relative identifiers.}

\item{normalise}{Logical; if TRUE, CURIEs/relative IRIs are expanded using
\code{prefixes} and \code{base_iri} before being stored.}
}
\value{
An object of class \code{"sh_node_shape"}.
}
\description{
Creates an S3 object representing a SHACL node shape. Node shapes define
constraints on focus nodes and typically reference one or more property
shapes via \code{sh:property}.
}
\examples{
person_shape <- sh_node_shape(
  id = "ex:PersonShape",
  targets = list(targetClass = "ex:Person")
)

}
