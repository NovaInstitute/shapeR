% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualiseSHACL.R
\name{visualiseSHACL}
\alias{visualiseSHACL}
\title{Visualise a SHACL shapes graph}
\usage{
visualiseSHACL(file, layout = "sugiyama", engine = c("ggraph", "visNetwork"))
}
\arguments{
\item{file}{Either a path to a SHACL file (Turtle, RDF/XML, JSON-LD, etc.) or
an object of class \code{sh_shape_graph} produced by \code{read_shacl()}.}

\item{layout}{Layout name passed to \code{ggraph} (e.g. "sugiyama", "fr").
Ignored when \code{engine = "visNetwork"}.}

\item{engine}{Visualisation engine to use. Defaults to \code{"ggraph"} for
static plots; set to \code{"visNetwork"} for an interactive widget.}
}
\value{
A \code{tidygraph::tbl_graph} object, returned invisibly.
The diagram is plotted as a side effect.
}
\description{
Parses a SHACL shapes graph (RDF) and produces a node–edge diagram
showing shapes, their property shapes, and nested shapes via \code{sh:node}.
}
\details{
Property nodes are labelled with:
\itemize{
\item \code{sh:path}
\item cardinality \code{[min..max]}
\item \code{sh:datatype}
\item \code{sh:class}
\item \code{sh:in} lists (values)
\item \code{sh:or} lists (alternative shapes)
}

Shape nodes are labelled with their shape ID and, if present,
\code{sh:targetClass}.
}
\examples{
\dontrun{
visualiseSHACL("inst/extdata/shapes.ttl")

prefixes <- c(
  ex   = "http://example.com/ns#",
  base = "http://example.com/base#"
)
visualiseSHACL(
  read_shacl(
    system.file("extdata", "visualise-shacl.ttl", package = "shapeR"),
    base_iri = "http://example.com/base#",
    prefixes = prefixes,
    normalise_iris = TRUE
  )
)
}

}
